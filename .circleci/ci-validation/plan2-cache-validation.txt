# Plan 2 CI validation — circle-cache.js builtins
#
# This file triggers the full PR workflow (.circleci/* global path filter).
# Used to validate cold vs warm cache behavior after removing install_cache_helpers_dependencies.
#
# cold: cache-version.txt bumped in same PR — forces fresh node_modules cache
# warm: re-run pipeline on this commit without cache-version change — state marker hit
#
# Remove this file after validation completes.

validation_state=warm
