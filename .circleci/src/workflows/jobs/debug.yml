resource_class: small
docker:
  - image: cimg/node:current
parameters:
  is_contributor_pr:
    type: boolean
    default: false
steps:
  - run:
      command: |
        echo "is_contributor_pr: << parameters.is_contributor_pr >>"