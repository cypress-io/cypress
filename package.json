{
  "name": "cypress-example",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "preinstall": "npm cache clean && rm -rf app && mkdir app && npm i cypress-example-kitchensink",
    "postinstall": "cp -r node_modules/cypress-example-kitchensink/app . && npm run build",
    "test": "cypress ci",
    "build": "gulp build",
    "deploy": "gulp deploy"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress-example-kitchensink": "cypress-io/cypress-example-kitchensink"
  },
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-clean": "^0.3.1",
    "gulp-gh-pages": "^0.5.4",
    "gulp-rev-all": "^0.8.22",
    "run-sequence": "^1.1.5"
  }
}
