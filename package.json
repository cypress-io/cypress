{
  "name": "cypress-monorepo",
  "version": "0.19.1",
  "main": "index.js",
  "bin": {
    "cypress": "bin/cypress"
  },
  "scripts": {
    "install-app": "node cli.js --exec install",
    "start": "npm run all",
    "watch-dev": "npm run all watch-dev",
    "test": "npm run all test -- --serial",
    "test-once": "npm run all test-once -- --serial",
    "test-unit-once": "npm run all test-unit-once -- --serial",
    "test-integration-once": "npm run all test-integration-once -- --serial",
    "test-e2e-once": "npm run all test-e2e-once -- --serial",
    "all": "node cli.js --exec run"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@cypress/npm-run-all": "^4.0.3",
    "ascii-table": "0.0.9",
    "eslint": "^3.7.1"
  },
  "dependencies": {
    "app-module-path": "^1.1.0",
    "bluebird": "^3.4.5",
    "chalk": "^1.1.3",
    "coffee-script": "^1.11.1",
    "glob": "^7.0.6",
    "lodash": "^4.16.3",
    "minimist": "^1.2.0"
  }
}
