machine:
  node:
    version: 5.1.1

dependencies:
  override:
    - npm install
    - npm install -g cypress-cli

test:
  pre:
    - npm run server:
        background: true
  override:
    - npm run build
    - cypress ci