test:
  pre:
    - npm run build

  override:
    - npm run test-server
    # - ./node_modules/.bin/gulp test:
    #     background: true
    # - sleep 6
    # - ./node_modules/.bin/mocha-phantomjs http://localhost:3500/specs/all_specs
