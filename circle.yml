machine:
  node:
    version: 6.1.0

test:
  pre:
    - npm run build

  override:
    - npm run test-unit-once:
        parallel: true
    - npm run test-integration-once:
        parallel: true
    - npm run test-e2e-once:
        parallel: true
  #   - npm run test-cov
  #   - npm run codecov
  #   - npm run coveralls