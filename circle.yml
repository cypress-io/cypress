machine:
  node:
    version: 6.1.0

dependencies:
  override:
    - npm install
    - npm install -g cypress-cli

test:
  pre:
    - npm start:
        background: true
  override:
    - npm run build
    - cypress ci
