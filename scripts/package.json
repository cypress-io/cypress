{
  "name": "internal-scripts",
  "version": "0.0.0-development",
  "scripts": {
    "lint": "eslint --ext .js,.ts,.json, .",
    "test": "mocha -r ../packages/ts/register --reporter spec 'unit/**/*spec.js'"
  }
}