import { mount } from 'cypress/react'
import HelloWorld from './HelloWorld.vue'

it('works', () => {
  console.log({ mount})
  // mount(HelloWorld, {
  //   props: {
  //     msg: 'Hello!'
  //   }
  // })

  // cy.contains('Hello!')
})
