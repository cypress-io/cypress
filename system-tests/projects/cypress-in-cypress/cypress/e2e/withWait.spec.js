describe('withWait', () => {
  it('passes', () => {
    cy.wait(2000)
    expect(true).to.eq(true)
  })

  it('passes', () => {
    cy.wait(2000)
    expect(true).to.eq(true)
  })

  it('passes', () => {
    cy.wait(2000)
    expect(true).to.eq(true)
  })

  it('passes', () => {
    cy.wait(2000)
    expect(true).to.eq(true)
  })
})
