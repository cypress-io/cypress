## v8-snapshot/require-full-path-var

The goal of this project is to load a module via a full path to the module. First snapshot `entry.js`. You can then load up the
app by running:

```
electron -r ./hook-require.js ./app.js
``` 
