describe('Nested Tests', () => {
  context('level 2', () => {
    context('level 3', () => {
      context('level 4', () => {
        context('level 5', () => {
          context('level 6', () => {
            context('level 7', () => {
              context('level 8', () => {
                context('level 9', () => {
                  context('level 10', () => {
                    context('level 11', () => {
                      context('level 12', () => {
                        context('level 13', () => {
                          context('level 14', () => {
                            context('level 15', () => {
                              context('level 16', () => {
                                context('level 17', () => {
                                  context('level 18', () => {
                                    context('level 19', () => {
                                      context('level 20', () => {
                                        it('passing test', () => {
                                          expect(true).to.be.true
                                        })
                                      })
                                    })
                                  })
                                })
                              })
                            })
                          })
                        })
                      })
                    })
                  })
                })
              })
            })
          })
        })
      })
    })
  })
})
