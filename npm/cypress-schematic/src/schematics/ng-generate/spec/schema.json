{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "cypress-schematics-e2e-spec",
  "title": "Cypress E2E Spec Options Schema",
  "type": "object",
  "properties": {
    "path": {
      "type": "string",
      "format": "path",
      "description": "The path where the component test will be created.",
      "visible": false
    },
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "$default": {
        "$source": "projectName"
      }
    },
    "name": {
      "type": "string",
      "description": "The name of the component test.",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What is the name of the component test?"
    },
    "testingType": {
      "enum": [
        "e2e",
        "component"
      ],
      "alias": "t",
      "description": "The type of test to generate - e2e or component. e2e is default.",
      "default": "e2e",
      "x-prompt": "What type of test do you want to generate?"
    }
  },
  "required": [
    "name",
    "testingType"
  ]
}