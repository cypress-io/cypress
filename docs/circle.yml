general:
  branches:
    ignore:
      - gh-pages

machine:
  node:
    version: 6.5.0

dependencies:
  post:
    - npm install -g cypress-cli

test:
  pre:
    - npm start:
        background: true
  override:
    - cypress run --record
