# gui = require('nw.gui')

# tray = new gui.Tray({ title: 'Tray', icon: 'img/logo.jpg' });

# tray.on "click", ->
#   console.log "clickeD!!!"

# # menu = new gui.Menu();
# # menu.append(new gui.MenuItem({ type: 'checkbox', label: 'box1' }));
# # tray.menu = menu;

# # // Remove the tray
# # tray.remove();
# # tray = null;