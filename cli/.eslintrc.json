{
  "extends": "../.eslintrc.js",
  "rules": {
    "no-restricted-syntax": [
      "error",
      {
        "selector": "CallExpression[callee.name='arch']",
        "message": "Do not use `arch()` to detect the user's machine architecture. Use utils.getRealArch() instead."
      },
      {
        "selector": "CallExpression[callee.object.name='process'][callee.property.name='arch']",
        "message": "Do not use `process.arch()` to detect the user's machine architecture. Use utils.getRealArch() instead."
      }
    ]
  }
}
