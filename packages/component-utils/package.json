{
  "name": "@packages/component-utils",
  "version": "0.0.0",
  "private": true,
  "main": "index.js",
  "scripts": {
    "build-prod": "tsc --project .",
    "clean-deps": "rm -rf node_modules",
    "semantic-release": "yarn semantic-release -e semantic-release-monorepo",
    "test": "yarn test-unit",
    "test-unit": "mocha --reporter mocha-multi-reporters --reporter-options configFile=../../mocha-reporter-config.json",
    "watch": "tsc -w --project ."
  },
  "dependencies": {},
  "devDependencies": {
    "@cypress/sinon-chai": "2.9.0",
    "react": "^16.13.1",
    "typescript": "3.5.3",
    "vue": "^2.6.11"
  },
  "files": [
    "lib"
  ],
  "types": "./lib/index.ts"
}
