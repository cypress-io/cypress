{
  "name": "@packages/component-utils",
  "version": "0.0.0",
  "private": true,
  "main": "index.js",
  "scripts": {
    "build-prod": "tsc --project .",
    "clean-deps": "rm -rf node_modules",
    "test": "yarn test-unit",
    "test-unit": "mocha test/unit --reporter mocha-multi-reporters --reporter-options configFile=../../mocha-reporter-config.json --compilers ts:@packages/ts/register,coffee:@packages/coffee/register --timeout 10000 --recursive --require test/pretest.ts",
    "watch": "tsc -w --project ."
  },
  "dependencies": {},
  "devDependencies": {
    "@cypress/sinon-chai": "2.9.0",
    "react": "^16.13.1",
    "typescript": "3.5.3",
    "vue": "^2.6.11"
  },
  "files": [
    "lib"
  ],
  "types": "./lib/index.ts"
}
