{
  "name": "@packages/component-utils",
  "version": "0.0.0",
  "private": true,
  "main": "index.js",
  "scripts": {
    "build-prod": "tsc --project .",
    "clean-deps": "rm -rf node_modules",
    "test": "yarn test-unit",
    "test-unit": "mocha test/unit --timeout 10000 --recursive -r @packages/ts/register -r @packages/coffee/register --reporter mocha-multi-reporters --reporter-options configFile=../../mocha-reporter-config.json",
    "watch": "tsc -w --project ."
  },
  "dependencies": {},
  "devDependencies": {
    "react": "^16.13.1",
    "typescript": "3.5.3",
    "vue": "^2.6.11"
  },
  "files": [
    "lib"
  ],
  "types": "./lib/index.ts"
}
