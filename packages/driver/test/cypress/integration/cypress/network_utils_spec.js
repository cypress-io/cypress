const $networkUtils = require('../../../../src/cypress/network_utils')

describe('src/cypress/network_utils', () => {
  context('#runSpecs', () => {
    let win

    beforeEach(() => {
      win = {
      }
    })

    it('fetches the resource via XHR', () => {

    })

    it('resolves the promise when it loads')

    it('rejects the promise when it errors')
  })
})
