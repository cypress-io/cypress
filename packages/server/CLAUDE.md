# Server

@AGENTS.md

Vitest-migrated unit tests (`test/unit/**/*.spec.{js,ts}`): use Vitest `expect` and `vi` only — do not add sinon/chai to new migrations unless an exceptional case is documented.
