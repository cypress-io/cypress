import { describe, it, expect } from 'vitest'

describe('FilterTaggedContent', () => {
  it('should filter tagged content', () => {
    //const filter = new FilterTaggedContent(/^ERROR:/, new Writable())
    expect(true).toBe(true)
  })
})
