import { describe, it, expect } from 'vitest'

describe('WriteToDebug', () => {
  it('should write to debug', () => {
    //const writeToDebug = new WriteToDebug()
    expect(true).toBe(true)
  })
})
