import { describe, it, expect } from 'vitest'

describe('FilterPrefixedContent', () => {
  it('should filter prefixed content', () => {
    //const filter = new FilterPrefixedContent(/^ERROR:/, new Writable())
    expect(true).toBe(true)
  })
})
