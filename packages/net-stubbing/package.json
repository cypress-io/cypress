{
  "name": "@packages/net-stubbing",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build-prod": "tsc --project . || true",
    "clean-deps": "rm -rf node_modules",
    "test": "mocha --reporter mocha-multi-reporters --reporter-options configFile=../../mocha-reporter-config.json"
  },
  "dependencies": {
    "concat-stream": "^2.0.0",
    "is-html": "^2.0.0",
    "minimatch": "^3.0.4",
    "throttle": "^1.0.3"
  },
  "devDependencies": {
    "bin-up": "1.2.0"
  },
  "files": [
    "lib",
    "server.js",
    "driver.js",
    "index.js"
  ]
}
